---
phase: 07-tray-enhancements
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified: [src-tauri/src/lib.rs, src-tauri/src/network.rs]
autonomous: true
requirements: [EXP-03]
must_haves:
  truths:
    - "System tray icon updates every second without manual intervention"
    - "Icon correctly reflects live download and upload speeds"
  artifacts:
    - path: "src-tauri/src/lib.rs"
      provides: "Background monitoring loop with tray integration"
  key_links:
    - from: "src-tauri/src/lib.rs"
      to: "src-tauri/src/icon_generator.rs"
      via: "icon generation"
---

<objective>
Refactor the background monitoring loop in `lib.rs` to calculate real-time speeds and update the system tray icon automatically every second.

Purpose: Automate tray updates to provide real-time feedback without UI polling.
Output: Integrated monitoring loop in `lib.rs` with tray icon hooks.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-tray-enhancements/07-CONTEXT.md
@src-tauri/src/lib.rs
@src-tauri/src/network.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement connection status detection</name>
  <files>src-tauri/src/network.rs, src-tauri/src/lib.rs</files>
  <action>
    Add a helper `is_online(networks: &Networks) -> bool` to `network.rs` (per D-05). 
    Check if any interface is up or has traffic.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>Online status detection implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor background loop for tray updates</name>
  <files>src-tauri/src/lib.rs</files>
  <action>
    Update the background thread in `run()` to:
    1. Calculate global download and upload speeds every second using `last_total_down/up` in `AppState`.
    2. Handle the `AppState` mutexes correctly to avoid deadlocks.
    3. Call `icon_generator::generate_tray_icon(down, up)` with the calculated speeds.
    4. Access the tray via `app.tray_by_id("main")` and use `tray.set_icon(Some(icon))` (per D-07).
    5. Handle the initial case where `last_total` values are 0.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>Monitoring loop now updates tray icon every second.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Monitoring Loop | Accesses `AppState` and interacts with Tauri tray API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Denial of Service | Background Loop | mitigate | Use `saturating_sub` for speed calculations to prevent overflow/underflow and ensure `sleep(1s)` is always called even if errors occur. |
</threat_model>

<success_criteria>
- Background monitoring loop calculates speed and updates tray icon every 1 second.
- App compiles and runs without crashes.
- Tray icon changes when network traffic occurs.
</success_criteria>

<output>
After completion, create `.planning/phases/07-tray-enhancements/07-02-SUMMARY.md`
</output>
