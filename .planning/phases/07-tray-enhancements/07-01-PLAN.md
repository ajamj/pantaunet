---
phase: 07-tray-enhancements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src-tauri/src/icon_generator.rs]
autonomous: true
requirements: [EXP-03]
must_haves:
  truths:
    - "Tray icon numbers are automatically scaled (e.g., '1.2M', '850K', '10G')"
    - "Download speed is displayed on the top half and Upload on the bottom half"
    - "Icons use high-contrast colors (white/indigo) on a transparent background"
  artifacts:
    - path: "src-tauri/src/icon_generator.rs"
      provides: "Numeric tray icon generation with unit scaling"
  key_links:
    - from: "src-tauri/src/icon_generator.rs"
      to: "ab_glyph"
      via: "font rendering"
---

<objective>
Update the icon generator to support numeric unit scaling and a refined stacked layout for real-time speed display in the system tray.

Purpose: Provide a clear, glanceable network speed indicator directly in the taskbar.
Output: Updated `icon_generator.rs` with robust formatting and rendering logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-tray-enhancements/07-CONTEXT.md
@src-tauri/src/icon_generator.rs

<interfaces>
From src-tauri/src/icon_generator.rs:
```rust
pub fn generate_tray_icon(down: &str, up: &str) -> Vec<u8>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement unit scaling for tray numbers</name>
  <files>src-tauri/src/icon_generator.rs</files>
  <behavior>
    - format_tray_speed(0) -> "0B"
    - format_tray_speed(1024) -> "1.0K"
    - format_tray_speed(1048576) -> "1.0M"
    - format_tray_speed(1073741824) -> "1.0G"
    - Max length should be 4-5 chars to fit the 32x32 icon half.
  </behavior>
  <action>
    Add a private helper function `format_tray_speed(bytes_per_sec: u64) -> String` that scales values to K, M, or G with one decimal point (per D-02). 
    If value is < 1024, return "0B" or the raw value if preferred, but "0B" is standard for idle.
  </action>
  <verify>
    <automated>cargo test --package pantaunet-lib --lib icon_generator::tests</automated>
  </verify>
  <done>Scaling logic implemented and tested with various speed values.</done>
</task>

<task type="auto">
  <name>Task 2: Refine stacked layout and rendering</name>
  <files>src-tauri/src/icon_generator.rs</files>
  <action>
    Update `generate_tray_icon` to:
    1. Accept `down: u64` and `up: u64` instead of strings.
    2. Use `format_tray_speed` to convert speeds to labels.
    3. Adjust `PxScale` to roughly 12.0 for better readability on 32x32.
    4. Position Download text in the top half (y around 12) and Upload text in the bottom half (y around 28) (per D-01).
    5. Use high-contrast colors: White (Rgba([255, 255, 255, 255])) for Download and a bright Indigo/LightBlue for Upload (per D-06).
    6. Maintain transparent background (per D-03).
  </action>
  <verify>
    <automated>cargo test --package pantaunet-lib --lib icon_generator::tests::test_generate_tray_icon_size</automated>
  </verify>
  <done>Tray icon rendering updated with stacked layout and high-contrast colors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal Logic | Speed values from monitoring loop are passed to the renderer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Denial of Service | `generate_tray_icon` | mitigate | Ensure image buffer allocation is minimal (32x32 is static) and font loading is efficient (use `lazy_static` or similar if needed, though current `include_bytes!` is fine). |
</threat_model>

<success_criteria>
- `generate_tray_icon` produces a valid 32x32 RGBA buffer with stacked numeric text.
- Unit scaling handles values from 0B to GB correctly.
- All tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/07-tray-enhancements/07-01-SUMMARY.md`
</output>
