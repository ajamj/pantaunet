---
phase: 07-tray-enhancements
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified: [src-tauri/src/lib.rs]
autonomous: true
requirements: [EXP-05]
must_haves:
  truths:
    - "Tray tooltip displays 'Online/Offline' status"
    - "Tray tooltip lists top 3 apps by bandwidth usage on hover"
    - "Tooltip formats speeds correctly (e.g., 5.2 MB/s)"
  artifacts:
    - path: "src-tauri/src/lib.rs"
      provides: "Dynamic tray tooltip generation"
  key_links:
    - from: "src-tauri/src/lib.rs"
      to: "tray.set_tooltip"
      via: "tooltip updates"
---

<objective>
Implement dynamic tooltip generation for the system tray, providing real-time status and a list of top 3 bandwidth-consuming applications.

Purpose: Provide detailed monitoring data at a glance via tray hover.
Output: Enhanced `lib.rs` with dynamic tooltip logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-tray-enhancements/07-CONTEXT.md
@src-tauri/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement top 3 apps extraction in monitoring loop</name>
  <files>src-tauri/src/lib.rs</files>
  <action>
    Modify the background loop to:
    1. Iterate through all processes (per D-04).
    2. Identify and sort the top 3 processes by current network usage (sum of read/write bytes since last refresh).
    3. Be efficient to minimize CPU overhead in the 1s loop.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>Top 3 apps logic implemented in background monitoring.</done>
</task>

<task type="auto">
  <name>Task 2: Implement dynamic tooltip generation</name>
  <files>src-tauri/src/lib.rs</files>
  <action>
    Create a helper to format the tooltip string as per the specific idea in `07-CONTEXT.md`:
    - Header: Pantaunet: [Online/Offline] (per D-05).
    - Speed Summary: Down: [X] MB/s | Up: [Y] MB/s.
    - Top List: 1. [App]: [Z] MB/s, etc.
    Call `tray.set_tooltip(Some(&tooltip_string))` every second in the loop.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>Dynamic tooltip generation active and updated every second.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tooltip Data | Process names and speeds are formatted into a string and shown to the user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Information Disclosure | Tooltip | accept | Showing top apps is the desired feature. If the user is concerned, they can disable it in settings (to be implemented in Phase 9). |
| T-07-03-02 | Denial of Service | Tooltip formatting | mitigate | Ensure the string formatting is efficient and top list is limited to 3 items. |
</threat_model>

<success_criteria>
- Hovering over the tray icon displays Online status and top 3 apps.
- Tooltip updates every second reflecting current usage.
- All speeds in tooltip match the tray icon and dashboard.
</success_criteria>

<output>
After completion, create `.planning/phases/07-tray-enhancements/07-03-SUMMARY.md`
</output>
